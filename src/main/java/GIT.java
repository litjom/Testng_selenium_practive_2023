public class GIT {


    /*
    git add.
    git commit - m ""
    git push --> to push to remote repor like git hub and git lab

    we can alos rebase in local
git pull - Youare pulling code from remote to Local working directory (eclipse), no involvement of local repo
So git pull = gitfetch +merge

git fetch - you are pulling form remote repo to local branch like the folder in your system then you have to do git merge master to
get it on the eclipse

     */
}
